# Static files cache policy (dynamic per-domain)
location /static/ {
    alias /var/www/$server_name/backend/app/static/;
    expires 30d;
    add_header Cache-Control "public, max-age=2592000, immutable";
}
